{
  "name": "node-ejs",
  "description": "Noli's Pixel Online",
  "main": "server.js",
  "scripts": {
    "watch": "parcel watch src/client.js -d dist",
    "build": "parcel build --no-cache src/client.js -d dist",
    "server": "node --experimental-modules server.mjs"
  },
  "dependencies": {
    "canvas": "*",
    "ejs": "^2.5.7",
    "express": "^4.16.2",
    "socket.io": "^2.0.4",
    "socket.io-client": "^2.0.4"
  }
}
